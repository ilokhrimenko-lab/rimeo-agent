<Window
    x:Class="RimeoAgent.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:views="using:RimeoAgent.Views"
    Title="Rimeo Agent">
    <Grid>
        <NavigationView x:Name="NavView"
                        IsBackButtonVisible="Collapsed"
                        PaneDisplayMode="Left"
                        IsPaneToggleButtonVisible="True"
                        OpenPaneLength="200"
                        SelectionChanged="NavView_SelectionChanged">
            <NavigationView.MenuItems>
                <NavigationViewItem Content="Library"  Tag="Library"  Icon="Library" />
                <NavigationViewItem Content="Analysis" Tag="Analysis" Icon="Audio"   />
                <NavigationViewItem Content="Pairing"  Tag="Pairing"  Icon="Link"    />
                <NavigationViewItem Content="Account"  Tag="Account"  Icon="Contact" />
                <NavigationViewItem Content="Logs"     Tag="Logs"     Icon="Document"/>
            </NavigationView.MenuItems>

            <NavigationView.FooterMenuItems>
                <NavigationViewItem Content="Quit" Tag="Quit" Icon="Cancel" />
            </NavigationView.FooterMenuItems>

            <Frame x:Name="ContentFrame" Margin="0" />
        </NavigationView>
    </Grid>
</Window>
