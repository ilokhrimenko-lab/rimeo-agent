<Window
    x:Class="RimeoAgent.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:views="using:RimeoAgent.Views"
    Title="Rimeo Agent">
    <Grid>
        <NavigationView x:Name="NavView"
                        IsBackButtonVisible="Collapsed"
                        PaneDisplayMode="Left"
                        IsPaneToggleButtonVisible="True"
                        OpenPaneLength="200"
                        SelectionChanged="NavView_SelectionChanged">
            <NavigationView.MenuItems>
                <NavigationViewItem Content="Library"  Tag="Library" />
                <NavigationViewItem Content="Analysis" Tag="Analysis" />
                <NavigationViewItem Content="Pairing"  Tag="Pairing" />
                <NavigationViewItem Content="Account"  Tag="Account" />
                <NavigationViewItem Content="Logs"     Tag="Logs" />
            </NavigationView.MenuItems>

            <NavigationView.FooterMenuItems>
                <NavigationViewItem Content="Quit" Tag="Quit" />
            </NavigationView.FooterMenuItems>

            <Frame x:Name="ContentFrame" Margin="0" />
        </NavigationView>
    </Grid>
</Window>
