<Page
    x:Class="RimeoAgent.Views.LibraryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Library" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,8"/>

        <!-- Search + reload -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8" Margin="0,0,0,8">
            <TextBox x:Name="SearchBox" PlaceholderText="Search…" Width="320"
                     TextChanged="SearchBox_TextChanged" />
            <Button Content="Reload" Click="Reload_Click" />
            <TextBlock x:Name="StatusLabel" VerticalAlignment="Center" Foreground="Gray" FontSize="12" />
        </StackPanel>

        <!-- Track list -->
        <ListView x:Name="TrackList" Grid.Row="2"
                  SelectionMode="Single"
                  IsItemClickEnabled="True">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="4,2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="200" />
                            <ColumnDefinition Width="80" />
                            <ColumnDefinition Width="60" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="{Binding Title}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                            <TextBlock Text="{Binding Artist}" Foreground="Gray" FontSize="12" TextTrimming="CharacterEllipsis"/>
                        </StackPanel>
                        <TextBlock Grid.Column="1" Text="{Binding Genre}" VerticalAlignment="Center"
                                   Foreground="Gray" FontSize="12" TextTrimming="CharacterEllipsis"/>
                        <TextBlock Grid.Column="2" Text="{Binding Key}" VerticalAlignment="Center"
                                   HorizontalAlignment="Center" FontSize="12"/>
                        <TextBlock Grid.Column="3" Text="{Binding BpmDisplay}" VerticalAlignment="Center"
                                   HorizontalAlignment="Right" FontSize="12"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
